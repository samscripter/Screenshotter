{
  "name": "screen-shotter",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/popup.jsx --bundle --outfile=popup.js --format=iife --target=chrome100",
    "watch": "esbuild src/popup.jsx --bundle --outfile=popup.js --format=iife --target=chrome100 --watch"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
